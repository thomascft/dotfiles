{
	"layer" : "top",
	"position" : "left", 
	"mode" : "dock",
	"modules-left" : ["custom/arch",  "river/tags",],
	"modules-center" : [],
	"modules-right" : ["pulseaudio", "backlight", "battery", "clock#hour", "clock#min"],
	"custom/arch" : {
		"format" : "",
		"on-click" : "eww windows | grep -i *dashboard && eww close dashboard || eww open dashboard"
	},

	"custom/previous#music" : {
		"format" : "ﭢ",
		"on-click" : "playerctl previous",
		"tooltip" : false
	},
	"custom/playpause#music" : {
		"format" : "{icon}",
		"return-type" : "json",
		"format-icons" : {
			"Playing": "",
			"Paused" : ""
		},
		"exec" : "playerctl metadata -f '{\"alt\" : \"{{status}}\"}' -F ",
		"on-click" : "playerctl play-pause",
		"tooltip" : false
	},
	"custom/next#music" : {
		"format" : "ﭠ",
		"on-click" : "playerctl next",
		"tooltip" : false
	},



	"river/tags" : {
		"num-tags" : 10,
		"tag-labels" : ["一", "二", "三", "四", "五", "六", "七", "八", "九", "十"]
		},
	"pulseaudio" : {
		"on-click" : "eww windows | grep -i *sliders && eww close sliders || eww open sliders",
		"format" : "{icon}",
		"format-muted" : "ﱝ",
		"format-icons" : {
			"headphone": "",
        	"hands-free": "﫽",
        	"headset": "﫽",
        	"default": ["", ""]
		}
	},
	"backlight" : {
		"on-click" : "eww windows | grep -i *sliders && eww close sliders || eww open sliders",
		"format" : "{icon}",
		"format-icons" : ["", "", "", "", "", "", ""]
	},
	"battery" : {
		"on-click" : "eww windows | grep -i *sliders && eww close sliders || eww open sliders",
        "format-icons" : ["", "", "", "", ""],
		"format" : "{icon}",
	},
	"clock#hour": {
		"on-click" : "eww windows | grep -i *sliders && eww close sliders || eww open sliders",
        "format": "{:%I}"
    },
	"clock#min": {
		"on-click" : "eww windows | grep -i *sliders && eww close sliders || eww open sliders",
        "format": "{:%M}"
    }
}
